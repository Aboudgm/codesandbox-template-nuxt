[build]
  base    = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

# SPA catch-all — React Router handles routing client-side
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
